project:
  name: "Integration test"
  output_dir: results

inputs:
  genotypes: genotypes.tsv
  annotations: null

snps:
  min_block_length: 1
  min_snp_flank: 0
  repeat_masking:
    library: null

viewer:
  dotplot_reference: null

advanced:
  sibeliaz:
    extra_options: []

  batching:
    blocks_per_chunk: 1

  alignment:
    mafft_options: []
